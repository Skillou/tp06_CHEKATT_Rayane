import { DirClientDirective } from './dir-client.directive';

describe('DirClientDirective', () => {
  it('should create an instance', () => {
    const directive = new DirClientDirective();
    expect(directive).toBeTruthy();
  });
});